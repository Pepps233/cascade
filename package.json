{
  "name": "cascade",
  "version": "0.1.0",
  "description": "Decompose a task into a DAG of subtasks executed by parallel CLI agents",
  "type": "module",
  "bin": {
    "cascade": "dist/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.19.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=22"
  }
}
